### To launch app using base container orchestration(docker-compose):

1. To start(add flag -d for detach mode)

```
docker-compose up
```

2. You can access app by typing "http://localhost:8080" in browser's address bar.

3. To finish

```
docker-compose down
```
