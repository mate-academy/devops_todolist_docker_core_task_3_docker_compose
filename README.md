1. To build and start both the MySQL database and Todolist app containers, use the following command: docker compose up -d
2. To stop and remove the containers, use the following command: docker compose down
